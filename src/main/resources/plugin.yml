main: ${main}.Main
version: $version
name: $name
author: skyneton

api-version: 1.13
folia-supported: true

commands:
  uuid:
    description: UUID Ban Command
    permission: server.uuid.ban.*
    usage: /<command>
